source 'https://rubygems.org'

gem 'rails', '3.2.9'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'


gem 'devise'
gem 'simple_form'
gem 'kaminari'
gem 'gravatar-ultimate'

#This gem provides a simple and extremely flexible way to upload files from Ruby applications. It works well with Rack based web applications, such as Ruby on Rails.
gem 'carrierwave' 


group :test, :development do
	gem 'sqlite3'
	# Faker libarary that generates fake data
	gem 'faker'

	gem 'nifty-generators'

	# factory girl is a fixtures replacement with a straightforward definition syntax, support for mutiple build strategies(
	#	saved instances, unsaved instances, attribute hashes, and stubbed objects), and support for multiple factories for the same class(user, admin_user
	#   and so on), including factory inheritance
	gem 'factory_girl_rails'
	# capybara for simulating user interaction with a web browser
	gem 'capybara'
	# Guard for running tests automatically
	gem 'guard-rspec'
end



# Gems used only for assets and not required
# in production environments by default.
group :assets do
	gem 'sass-rails',   '~> 3.2.3'
	gem 'coffee-rails', '~> 3.2.1'
	gem 'bootstrap-sass-rails'
	# See https://github.com/sstephenson/execjs#readme for more supported runtimes
	# gem 'therubyracer', :platforms => :ruby

	gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'


# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
